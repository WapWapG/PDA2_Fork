// // ════════════════════════════════════════════════════════
// //  StubApp — минимальный тест-стенд для изоляции CRASH-1.
// //  Один статичный лейбл, никакой другой логики. Задача —
// //  проверить, воспроизводится ли краш (см. дока, CRASH-1)
// //  при единственном заведомо корректном приложении.
// //
// //  ВНИМАНИЕ: сигнатуры методов базового класса взяты из
// //  описания в доке ("Приложения и фреймворк"), не из
// //  реального PDA2App.h — этот файл ещё не был показан.
// //  Если что-то не совпадёт при компиляции — пришли
// //  PDA2App.h и/или ClockApp.h для сверки.
// // ════════════════════════════════════════════════════════

// #pragma once

// #include <PDA2App.h>

// class StubApp : public PDA2App {
// public:
//     void onInit() override;
//     void onOpen() override;
//     void onClose() override;
//     void onTick(uint32_t delta_ms) override;
//     // onBackground() не переопределяем — пустой дефолт, как у остальных 4 приложений
//     // onKey() не переопределяем — StubApp не реагирует на ввод

// private:
//     lv_obj_t* _label = nullptr;
// };